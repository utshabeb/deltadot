import React from 'react';
import type { PR } from '../types.js';
export declare function PRsView({ prs, syncingRepoNames, selectedIdx, loading, error, frame, width }: {
    prs: PR[];
    syncingRepoNames: string[];
    selectedIdx: number;
    loading: boolean;
    error: string;
    frame: number;
    width: number;
}): React.JSX.Element;
//# sourceMappingURL=PRsView.d.ts.map