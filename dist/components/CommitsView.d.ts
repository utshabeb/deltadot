import React from 'react';
import type { RepoState } from '../types.js';
export declare function CommitsView({ repo, base, release, selectedCommitIdx, frame, width }: {
    repo: RepoState;
    base: string;
    release: string;
    selectedCommitIdx: number;
    frame: number;
    width: number;
}): React.JSX.Element;
//# sourceMappingURL=CommitsView.d.ts.map