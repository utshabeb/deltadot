import React from 'react';
export declare function TopBar({ workspace, base, release, repoCount, syncingCount, doneCount, lastSync, frame, width }: {
    workspace: string;
    base: string;
    release: string;
    repoCount: number;
    syncingCount: number;
    doneCount: number;
    lastSync: string;
    frame: number;
    width: number;
}): React.JSX.Element;
//# sourceMappingURL=TopBar.d.ts.map